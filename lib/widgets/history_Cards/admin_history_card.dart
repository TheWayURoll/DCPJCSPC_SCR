import 'package:flutter/material.dart';

class AdminHistoryCard extends StatelessWidget {
  const AdminHistoryCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}